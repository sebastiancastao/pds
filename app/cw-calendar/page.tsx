// /cw-calendar reuses the global-calendar implementation in CW mode: it shows only
// CWT Trailers (division==='trailers') events, and events created from here are CW events.
export { default } from "../global-calendar/page";
