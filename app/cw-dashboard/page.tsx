// /cw-dashboard mirrors the /dashboard implementation in CW mode: it shows only
// CWT Trailers (division==='trailers') events, and events created from here are CW
// events. CW (trailers division) users are routed here after login.
export { default } from "../dashboard/page";
